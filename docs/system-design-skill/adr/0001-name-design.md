# Name the Skill `design`

The active skill should be named `design`. The user explicitly chose this shorter name for the greenfield system and service design workflow.

## Considered Options

- `system-design` - initially chosen because it was explicit about system architecture, but replaced by the user's preferred shorter name.
- `design` - chosen because it is concise and user-preferred for invoking the design workflow.

## Consequences

The implemented skill folder should be `skills/design/`, and README should list `design` as an active generic skill.
