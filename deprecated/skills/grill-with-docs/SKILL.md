---
name: grill-with-docs
description: Deprecated planning interview skill preserved for reference. Use `plan` for current work.
---

# Deprecated: Grill With Docs

This skill has been absorbed into `skills/plan`.

The original workflow interviewed the user one question at a time, challenged terminology against `CONTEXT.md`, and offered ADRs for durable decisions. The current `plan` skill keeps that behavior but writes work-packet artifacts under `docs/<jira-ticket-or-context>/` instead of root-level planning files.
